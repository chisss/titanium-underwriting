# Underwriting 模块设计说明

## 设计目标

根据出单或保全风险特征和核保规则生成核保案件及结论，为 Policy 和 Maintenance 提供可审计的承保决策。

## 方案选择

- 特征提取由 Feature Center 提供，复杂规则由 Rule Engine 执行；Underwriting 只负责核保领域编排和结果聚合。
- 自动核保和人工核保共享案件状态模型，通过事件通知 Policy。
- 核保结论与规则版本、请求号和租户绑定，支持重试和追溯。
- 保全核保使用独立正式 API；输入冻结保单基准版本、产品/计划版本、配置证据和结构化风险字段差异，不复用新单核保请求。

## 关键决策

- 规则执行失败默认不放行，进入可重试或人工处理状态。
- 查询投影异步更新，DLQ 记录无法投影的事件，不吞掉异常。
- 保全核保以 `tenantId + idempotencyKey` 生成确定性 `MUW-*` 案件号，聚合复算载荷 SHA-256；同键同载荷幂等返回，同键异载荷拒绝。
- 已识别自动规则返回通过、附加条件通过或拒绝；人工分类及未知分类统一返回 `MANUAL_REVIEW`，只有配置无需核保且零风险差异时返回 `NOT_REQUIRED`。

## 安全边界

核保数据按 tenantId 隔离；证件、健康等敏感字段只在授权范围内传输和记录。服务端不信任调用方提交的载荷摘要，
聚合按结构化请求重新计算；对外响应仅返回核保案件号、版本化结论、附加条件和脱敏摘要，不回显原始风险字段值。

## 已知限制

当前保全规则分类为版本化验收实现，生产 Rule Engine 编排、人工核保工作台/队列、最终结论回写和复杂特征加工仍需补充。

## 变更历史

### 2026-08-19 - 补齐模块设计记录

明确 Feature Center、Rule Engine 与核保域的职责边界及失败关闭策略。

### 2026-08-19 - 修复产品核保配置引用

核保域改为按产品编码调用 Product 专用查询接口，避免把险种编码误传给 productId 路径后静默降级。

### 2026-08-25 - M4-04 保全核保正式契约

新增独立保全核保 API、确定性核保案件、结构化载荷摘要复算、版本化结论及正式 HTTP Provider。未知风险失败关闭转
人工，不自动放行；Maintenance 可依赖稳定契约执行幂等重试和回显校验，不修改既有新单核保 API。
